# When running the Next.js dev server alongside the FastAPI backend locally,
# set this so the frontend knows where to send API requests.
# Leave empty (or unset) in production — the frontend is served from the same origin.
NEXT_PUBLIC_API_URL=http://localhost:8000
